name: Daily Instagram Post

on:
  schedule:
    - cron: '0 8 * * *'
  
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (no posting)'
        required: false
        default: false
        type: boolean

jobs:
  post:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          
      - name: Install Arabic fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-core fonts-noto-extra fonts-amiri
          fc-cache -f -v
          
          # Vérifier les polices installées
          fc-list | grep -i "amiri\|noto.*arab" || echo "Fonts installed"
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pillow pandas requests
          pip install arabic-reshaper python-bidi
          pip install -r requirements.txt

          # Vérifier que les packages arabes sont installés
          python -c "import arabic_reshaper; print('✅ arabic-reshaper OK')"
          python -c "from bidi.algorithm import get_display; print('✅ python-bidi OK')"
      
      - name: Run daily post
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_BUSINESS_ID: ${{ secrets.IG_BUSINESS_ID }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          if [ "${{ inputs.dry_run }}" == "true" ]; then
            python src/main.py --dry-run
          else
            python src/main.py
          fi
      
      - name: Commit and push changes
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          
          git add -A
          
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📸 Posted: $(date +'%Y-%m-%d')"
            git pull --rebase origin main
            git push origin main
          fi
